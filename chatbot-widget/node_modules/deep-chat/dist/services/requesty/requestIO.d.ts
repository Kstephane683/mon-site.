import { RequestyToolCall } from '../../types/requestyInternal';
import { RequestyAPIResult } from '../../types/requestyResult';
import { MessageContentI } from '../../types/messagesInternal';
import { Requesty } from '../../types/requesty';
import { Messages } from '../../views/chat/messages/messages';
import { Response as ResponseI } from '../../types/response';
import { DirectServiceIO } from '../utils/directServiceIO';
import { DeepChat } from '../../deepChat';
export declare class RequestyIO extends DirectServiceIO {
    insertKeyPlaceholderText: string;
    keyHelpUrl: string;
    url: string;
    permittedErrorPrefixes: string[];
    readonly _streamToolCalls?: RequestyToolCall[];
    constructor(deepChat: DeepChat);
    private static getAudioContent;
    private static getContent;
    private preprocessBody;
    callServiceAPI(messages: Messages, pMessages: MessageContentI[]): Promise<void>;
    extractResultData(result: RequestyAPIResult, prevBody?: Requesty): Promise<ResponseI>;
    private extractStreamResult;
}
//# sourceMappingURL=requestIO.d.ts.map